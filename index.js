// DESAFIO CALCULO IMC

const nome = 'Juan'
const peso = 1
const altura = 1.3
const sexo = 'transexual'

const imc = (peso / altura) * altura

if (imc >= 30) {
  console.log('Juan você está gordo pra krl')
} else {
  console.log('Juan você ta magrão')
}
